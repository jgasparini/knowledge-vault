# Regression baseline history — article-basic

One line per approved baseline move: date, then a one-sentence reason.

- 2026-08-05 — initial baseline, first bootstrap run of Mode 6.
- 2026-08-05 — widened root-index-delta.md to also capture the Projects-table row (previously Global-Resources-only scoping produced a false-positive regression signal on any future correct run).
